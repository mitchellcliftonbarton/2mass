import Ember from 'ember';

export default Ember.Route.extend({
    // beforeModel() {
    //     this.replaceWith('home');
    // }

    // model(params) {
    //   if (params.id !== 'index') {
    //     this.set('templateName', 'show-page');
    //     console.log(params);
    //   }
    //
    //   return this.get('store').findRecord('show', params.id);
    // }
});
